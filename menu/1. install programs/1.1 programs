#!/bin/bash
export Lock="1"
cd "${Path_Install}/programs"
source ${Path_Install}/litenavi/litenavi.sh
Programs=$(source ${Path_Install}/litenavi/litenavi.sh)
printf "%s" "${Programs}"
sleep 4
pacman -Sy ${Programs}
