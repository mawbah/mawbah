#!/bin/bash
export Lock="1"
cd "${Path_Install}/programs"
source ${Path_Install}/litenavi/litenavi.sh
#for Program in $(cat ${Path_Install}/prog_list); do
#    ${Path_Install}/programs/${Program}/i.sh
#done
echo !!!!!!!"${LN_Output[@]}"!!!!!!!!1
